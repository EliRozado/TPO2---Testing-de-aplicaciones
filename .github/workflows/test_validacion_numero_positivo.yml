name: Test Validacion Numero Positivo
on: 
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python 3.14
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run tests and generate HTML report
        run: |
          python -m pytest testing/test_validacion_numero_positivo.py --html=report-validacion_numero_positivo.html --self-contained-html

      - name: Upload HTML report
        uses: actions/upload-artifact@v7
        with:
          name: report-validacion_numero_positivo
          path: report-validacion_numero_positivo.html